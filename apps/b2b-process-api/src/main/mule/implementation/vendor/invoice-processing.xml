<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
	<sub-flow name="inbound-invoice-processing-subflow" doc:id="0f3b0ec8-ab83-4254-a92f-ce1b7e4e5fca" >
		<logger level="INFO" doc:name="LOG INFO : START" doc:id="f3eaae67-8060-4585-9b21-8949893def16" message='#[%dw 2.0&#10;output application/json&#10;---&#10;{&#10;	event: "START",&#10;	flow: flow.name,&#10;	application: app.name,&#10;	environment: Mule::p("mule.env"),&#10;	timestamp: now(),&#10;	correlationId: vars.integration.correlationId,&#10;	transmissionId: vars.initialPayload[0].b2bMessage.header.transmissionId default "",&#10;	sourceSystem: "APM - Anypoint Partner Manager",&#10;	targetSystem: "P21"&#10;}]'/>
		<flow-ref doc:name="Flow Reference" doc:id="0d3cac58-dffa-4c24-8888-f27ed40d3784" name="get-email-view-subflow"/>
		<ee:transform doc:name="var : httpOperation - get purchase order" doc:id="0f587fa5-6fc6-4c33-aa38-3c2478ca3fe8" >
			<ee:message >
			</ee:message>
			<ee:variables >
				<ee:set-variable resource="dwl/inbound-invoice-processing-subflow/v_httpOperationGetPurchaseOrder.dwl" variableName="httpOperation" />
			</ee:variables>
		</ee:transform>
		<flow-ref doc:name="Flow Reference To commom-http-request-subflow" doc:id="fe068a4b-652e-4c31-a9e9-4aac018cbed3" name="common-http-request-subflow" target="purchaseOrderData"/>
		<logger level="INFO" doc:name="LOG INFO : P21 View Data" doc:id="6be8d4ce-9b7c-40e6-8a9a-d1d44551958b" message='#[%dw 2.0&#10;output application/json indent = false&#10;---&#10;{&#10;	"Message": "P21 View",&#10;	"Payload": vars.purchaseOrderData&#10;}]'/>
		<ee:transform doc:name="var : isValid" doc:id="866e5918-c2cb-45fd-b79c-fddd23c0c465" >
			<ee:message >
			</ee:message>
			<ee:variables >
				<ee:set-variable resource="dwl/inbound-invoice-processing-subflow/v_isValid.dwl" variableName="isValid" />
			</ee:variables>
		</ee:transform>
		<choice doc:name="isValid Invoice?" doc:id="79afbb3c-188a-4276-acd4-2c9d7bb69503">
			<when expression="#[vars.isValid.isValid == false&#10;&#10;or vars.isValid.validationErrors.shipToNameMatchPercentage &lt; (Mule::p('shipToValidation.lowerLimit') as Number)&#10;&#10;or vars.isValid.validationErrors.shipToAddressMatchPercentage &lt; (Mule::p('shipToValidation.lowerLimit') as Number)]">
				<raise-error doc:name="CUSTOM:VALIDATION_FAILED" doc:id="7ead687f-13fd-40c6-87a7-c586405347d4" type="CUSTOM:VALIDATION_FAILED" description="Validation Failed" />
			</when>
			<otherwise>
				<logger level="INFO" doc:name="LOG INFO : Valid Invoice" doc:id="5f5af8b4-e52c-4e60-850d-cde8a78887e3" message='#[%dw 2.0&#10;output application/json&#10;---&#10;{&#10;	flow: flow.name,&#10;	application: app.name,&#10;	environment: Mule::p("mule.env"),&#10;	timestamp: now(),&#10;	correlationId: vars.integration.correlationId,&#10;	transmissionId: vars.initialPayload[0].b2bMessage.header.transmissionId default "",&#10;	"message": "Invoice validation successful, Moving forward to create the Invoice in p21",&#10;}]' />
			</otherwise>
		</choice>
		<ee:transform doc:name="var : p21Invoice" doc:id="4dc242d7-5131-44a4-a93a-f72e5a3cc70d" >
			<ee:message >
			</ee:message>
			<ee:variables >
				<ee:set-variable resource="dwl/inbound-invoice-processing-subflow/v_p21Invoice.dwl" variableName="p21Invoice" />
			</ee:variables>
		</ee:transform>
		<ee:transform doc:name="var : httpOperation - post invoice" doc:id="8d832f08-d5bd-4035-bb98-a4041efdda0c">
			<ee:message />
			<ee:variables>
				<ee:set-variable resource="dwl/inbound-invoice-processing-subflow/v_httpOperationPostInvoice.dwl" variableName="httpOperation" />
			</ee:variables>
		</ee:transform>
		<flow-ref doc:name="Flow Reference To commom-http-request-subflow" doc:id="98913654-3e07-4c01-be00-d71faa614beb" name="common-http-request-subflow" />
		<logger level="INFO" doc:name="LOG INFO : P21 Response Payload" doc:id="b58c0223-9472-440d-bae4-b40e566691d1" message='#[%dw 2.0&#10;output application/json indent = false&#10;---&#10;{&#10;	"Message": "P21 Response Payload After Invoice Creation",&#10;	"Payload": payload&#10;}]'/>
		<choice doc:name="is Successful?" doc:id="4a8c246b-d93e-451f-afbd-f3e1e84a071b" >
			<when expression='#[payload."TransactionSetResult"."Summary"."Failed" != "0"]'>
				<raise-error doc:name="CUSTOM:P21_FAILED" doc:id="cf33fd6d-514a-40df-8113-45bedd343499" type="CUSTOM:P21_FAILED" description='#[payload."TransactionSetResult"."Messages".*"string" joinBy "\n"]'/>
			</when>
			<otherwise >
				<logger level="INFO" doc:name="LOG INFO : InvoiceCreated" doc:id="a929f772-d691-425a-abf8-8333ff62d353" message='#[%dw 2.0&#10;output application/json&#10;---&#10;{&#10;	flow: flow.name,&#10;	application: app.name,&#10;	environment: Mule::p("mule.env"),&#10;	timestamp: now(),&#10;	correlationId: vars.integration.correlationId,&#10;	transmissionId: vars.initialPayload[0].b2bMessage.header.transmissionId default "",&#10;	"message": "Invoice Creation Successful"&#10;}]'/>
			</otherwise>
		</choice>
		<choice doc:name="Send Shipment Address Warning Email?" doc:id="74f2d4e7-a8cf-4a84-9bc1-51f0b9c52a20" >
			<when expression="#[(vars.isValid.validationErrors.shipToNameMatchPercentage &gt;= ((Mule::p('shipToValidation.lowerLimit')) as Number) and vars.isValid.validationErrors.shipToNameMatchPercentage &lt;=((Mule::p('shipToValidation.upperLimit')) as Number))&#10;or&#10;(vars.isValid.validationErrors.shipToAddressMatchPercentage &gt;= ((Mule::p('shipToValidation.lowerLimit')) as Number) and vars.isValid.validationErrors.shipToAddressMatchPercentage &lt;=((Mule::p('shipToValidation.upperLimit')) as Number))]">
				<ee:transform doc:name="Prepare Warning Email Payload, var : subject" doc:id="3de95ddf-6440-4783-91d0-c609384b4a26">
			<ee:message>
				<ee:set-payload><![CDATA[%dw 2.0
output text/plain
 
fun safe(v, d = "N/A") =
    if (v == null or (v is String and trim(v) == "")) d else v
 
var integration = vars.integration default vars.initialVariables default {}
var cdm = (vars.initialPayload default [])[0].b2bMessage default {}
var header = cdm.header default {}
 
var senderKey = header.senderId default ""
 
var vendorId =
    if (senderKey != "")
        (p("partner.inbound." ++ senderKey)
            default p("partner.outbound." ++ senderKey)
            default "N/A")
    else
        "N/A"
 
var poNumber = safe(header.poNumber)
 
var namePercentage =
    vars.isValid.validationErrors.shipToNameMatchPercentage default null
 
var addressPercentage =
    vars.isValid.validationErrors.shipToAddressMatchPercentage default null
 
var lowerLimit =
    (Mule::p("shipToValidation.lowerLimit") as Number)
 
var upperLimit =
    (Mule::p("shipToValidation.upperLimit") as Number)
 
var data = {
    lowerLimit: lowerLimit as String,
    upperLimit: upperLimit as String,
 
    shipToNameMatchPercentage:
        if (namePercentage != null)
            ((namePercentage as Number) as String {format: "00.00"}) ++ "%"
        else
            "N/A",
 
    shipToAddressMatchPercentage:
        if (addressPercentage != null)
            ((addressPercentage as Number) as String {format: "00.00"}) ++ "%"
        else
            "N/A",
 
    flowDirection:
        if (lower(safe(integration.source, "APM")) == "apm")
            "INBOUND"
        else
            "OUTBOUND",
 
    documentType:
        safe(integration."integration-type", safe(header.documentType, "API")),
 
    warningTitle: "Ship-To Validation Warning",
 
    bannerColor: "#f59e0b",
 
    warningType: "SHIP_TO_VALIDATION_WARNING",
 
    warningCategory: "SHIP_TO_VALIDATION",
 
    status: "PROCESSED WITH WARNING",
 
    companyName:
        vars.purchaseOrderData.value.company_no[0] default "N/A",
 
    vendorName:
        vendorId,
 
    route:
        safe(integration.source, "APM")
        ++ " → Mule → "
        ++ safe(integration.target, "P21"),
 
    businessKey:
        "PO " ++ poNumber,
 
	timestamp:
    (now() >> "UTC") as String {format: "yyyy-MM-dd HH:mm:ss 'UTC'"},
 
    transmissionId:
        safe(header.transmissionId),
 
    keyLabel:
        "Correlation ID",
 
    key:
        correlationId default uuid(),
 
    transactionType:
        safe(integration."integration-type", safe(header.documentType, "API")),
 
    appName:
        p("api.name") default "Mule Application",
 
    environment:
        upper(p("mule.env") default "DEV"),
 
    message:
        "Purchase Order "
        ++ poNumber
        ++ " was processed successfully with a warning.",
 
    warningDescription:
        "Ship-To validation results:"
        ++ "\n• Ship-To Name Match Percentage: "
        ++ (
            if (namePercentage != null)
                ((namePercentage as Number) as String {format: "00.00"})
            else
                "N/A"
        )
        ++ "%"
        ++ "\n• Ship-To Address Match Percentage: "
        ++ (
            if (addressPercentage != null)
                ((addressPercentage as Number) as String {format: "00.00"})
            else
                "N/A"
        )
        ++ "%"
        ++ "\n\nThe transaction was processed because one or more Ship-To validation match percentages fell within the configured warning threshold range of "
        ++ (lowerLimit as String)
        ++ "% and "
        ++ (upperLimit as String)
        ++ "%.",
 
    warningResolution:
        "No action is required. Please review the Ship-To Name and Ship-To Address information if necessary."
}
 
var template =
    readUrl(
        "classpath://templates/warning-template.html",
        "text/plain"
    )
 
---
template replace /\$\{(\w+)\}/ with ((m) -> (data[m[1]] as String) default "")]]></ee:set-payload>
			</ee:message>
					<ee:variables >
						<ee:set-variable variableName="subject" ><![CDATA[%dw 2.0
output application/json
 
var env = upper(p("mule.env") )
 
var integrationType =
    vars.integration."integration-type"
        default vars.initialVariables."integration-type"
        default "Transaction"
 
var poNumber =
    vars.initialPayload[0].b2bMessage.header.poNumber
        default "N/A"
 
 
---
[
    env,
    "FCG WARNING",
    integrationType,
    "PO " ++ poNumber
]
    filter ($ != null and $ != "")
    joinBy " | "]]></ee:set-variable>
					</ee:variables>
		</ee:transform>
				<flow-ref doc:name="Flow Reference to send-email-subflow" doc:id="eff23e8b-1362-47d3-aa38-6d3fba9ffa35" name="send-email-subflow" />
			</when>
		</choice>
		<flow-ref doc:name="Flow Reference To get-email-view-subflow" doc:id="7e3c238a-7c16-4342-a4cf-37a16ff1f669" name="get-email-view-subflow" target="emailView" />
		<ee:transform doc:name="var : sendSuccessMail" doc:id="6a008995-d7e4-4860-972a-6d3b179d6c44">
			<ee:message />
			<ee:variables>
				<ee:set-variable variableName="sendSuccessMail" ><![CDATA[%dw 2.0

output application/json

var vendorName = [(p('partner.inbound.' ++ (vars.initialPayload[0].b2bMessage.header.senderId default "" as String)))]
    default [(p('partner.outbound.' ++ (vars.initialPayload[0].b2bMessage.header.senderId default "" as String)))]
    default []
 
var viewRecords =

    (vars.emailView.value default [])

        filter ((vendorName default []) contains $.trading_partner_name)
 
---
viewRecords."email_to_biz" contains "Y"
]]></ee:set-variable>
			</ee:variables>
		</ee:transform>
		<choice doc:name="Choice" doc:id="1319613a-4a52-4534-9c3f-fc883545bcd3" >
			<when expression='#[vars.sendSuccessMail == true]'>
				<ee:transform doc:name="Prepare Success Email Payload, var : subject" doc:id="357d431d-1508-4a4d-a77b-426bf7d5a3a6" >
					<ee:message >
						<ee:set-payload ><![CDATA[%dw 2.0
output text/plain
 
fun safe(v, d = "N/A") =
    if (v == null or (v is String and trim(v) == "")) d else v
 
var integration = vars.integration default vars.initialVariables default {}
var sysInfo = vars.systemInformation default {}
var cdm = (vars.initialPayload default [])[0].b2bMessage default {}
var header = cdm.header default {}
 
var senderKey = header.senderId default ""
var vendorId =
    if (senderKey != "")
        (p("partner.inbound." ++ senderKey)
            default p("partner.outbound." ++ senderKey)
            default "N/A")
    else
        "N/A"
 
var poNumber = safe(header.poNumber)
  
var data = {
    flowDirection:
        if (lower(safe(integration.source, "APM")) == "apm")
            "INBOUND"
        else
            "OUTBOUND",
 
    documentType:
        safe(
            integration."integration-type",
            safe(header.documentType, "API")
        ),
 
    successTitle: "P21 Processing Completed",
 
    bannerColor: "#16a34a",
 
    status: "SUCCESS",
 
    statusMessage:
            "P21 Processing Completed Successfully",

 
    companyName:
        vars.purchaseOrderData.value.company_no[0]
            default "N/A",
 
    vendorName:
        vendorId,
 
    route:
        safe(integration.source, "APM")
        ++ " → Mule → "
        ++ safe(integration.target, "P21"),
 
    businessKey:
        "PO " ++ poNumber,
 
	timestamp:
    (now() >> "UTC") as String {format: "yyyy-MM-dd HH:mm:ss 'UTC'"},
 
    transmissionId:
        safe(header.transmissionId),
 
    keyLabel:
        "Correlation ID",
 
    key:
        correlationId default uuid(),
 
    transactionType:
        safe(
            integration."integration-type",
            safe(header.documentType, "API")
        ),
 
    appName:
        p("api.name")
            default "Mule Application",
 
    environment:
        upper(p("mule.env") default "DEV"),
 
    message:
        "Invoice for Purchase Order "
        ++ poNumber
        ++ " has been processed successfully in P21.",
 
    p21OrderNumber:
        poNumber,
 
    statusCode: "200"
}
 
var template =
    readUrl(
        "classpath://templates/success-template.html",
        "text/plain"
    )
 
---
template replace /\$\{(\w+)\}/ with ((m) -> (data[m[1]] as String) default "")]]></ee:set-payload>
					</ee:message>
					<ee:variables >
						<ee:set-variable variableName="subject" ><![CDATA[%dw 2.0
output text/plain
 
var env = upper(p("mule.env") default "DEV")
 
var integrationType =
    vars.integration."integration-type"
        default vars.initialVariables."integration-type"
        default "Transaction"
 
var poNumber =
    vars.initialPayload[0].b2bMessage.header.poNumber
        default "N/A"
 
var vendorName =
    (p('partner.inbound.' ++ (vars.initialPayload[0].b2bMessage.header.senderId default "" as String)))
    default (p('partner.outbound.' ++ (vars.initialPayload[0].b2bMessage.header.senderId default "" as String)))
    default "N/A"
 
---
[
    env,
    "FCG SUCCESS",
    integrationType,
    vendorName,
    "PO " ++ poNumber
]
filter ($ != null and $ != "")
joinBy " | "]]></ee:set-variable>
					</ee:variables>
				</ee:transform>
				<flow-ref doc:name="Flow Reference to send-email-subflow" doc:id="a10db513-bff6-44ac-b762-f8817b7621d3" name="send-email-subflow" />
			</when>
		</choice>
		<logger level="INFO" doc:name="LOG INFO : END" doc:id="7a218d2e-888d-442d-8952-b5cdd14450e9" message='#[%dw 2.0&#10;output application/json&#10;---&#10;{&#10;	event: "END",&#10;	flow: flow.name,&#10;	application: app.name,&#10;	environment: Mule::p("mule.env"),&#10;	timestamp: now(),&#10;	correlationId: vars.integration.correlationId,&#10;	transmissionId: vars.initialPayload[0].b2bMessage.header.transmissionId default "",&#10;	//payloadSizeBytes: sizeOf(write(payload, "application/json")),&#10;	sourceSystem: "APM - Anypoint Partner Manager",&#10;	targetSystem: "P21"&#10;}]'/>
	</sub-flow>
</mule>
